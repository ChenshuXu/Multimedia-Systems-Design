To run the code from command line, first compile with:

>> javac ImageDisplay.java

and then, you can run it to take in two parameters "pathToImage" and "n":

>> java ImageDisplay pathToImage n

The middle image is DCT result, right image is DWT result